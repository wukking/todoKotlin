<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/nav_home">

    <fragment android:id="@+id/nav_home"
        android:name="com.wuyson.todokotlin.ui.fragment.HomeFragment"
        android:label="nav_home"
        tools:layout="@layout/fragment_home"/>

    <fragment android:id="@+id/nav_device"
        android:name="com.wuyson.todokotlin.ui.fragment.DeviceFragment"
        android:label="nav_device"
        tools:layout="@layout/fragment_device"/>

    <fragment android:id="@+id/nav_user"
        android:name="com.wuyson.todokotlin.ui.fragment.UserFragment"
        android:label="nav_user"
        tools:layout="@layout/fragment_user"/>
</navigation>